service: MyService

provider:
  name: foobar
  runtime: nodejs

functions:
  foo1:
    handler: handler.foo1
    events:
      - http:
          path: foo
          method: get
  foo2:
    handler: handler.foo2
    events:
      - http: post foo2
