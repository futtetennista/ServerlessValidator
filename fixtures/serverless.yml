service: MyService

provider:
  name: foobar
  runtime: nodejs

functions:
  foo:
    handler: handler.foo
    events:
      - http:
          path: foo
          method: get
  bar:
    handler: handler.bar
